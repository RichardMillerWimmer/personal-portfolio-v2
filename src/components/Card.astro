---
import styles from '../styles/Card.module.scss'

interface CardProps {
    title: string
    description: string
    image: string
    url?: string
}

const { title, description, image, url  } = Astro.props as CardProps

const RenderTag = url ? 'a' : 'div'
---
<style>
    h3 {
        margin-bottom: 15px;
    }
</style>

<div class={styles.wrapper}>
    <RenderTag class={styles.card} href={url}>
        <div class={styles.container}>
            <div class={styles.textContainer}>
                <h3>{title}</h3>
                <p>{description}</p>
            </div>
            <div class={styles.imageContainer}>
                <img src={image} alt={title}/>
            </div>
        </div>
    </RenderTag>
</div>